

class authController{

    createUser = (req, res, next)=> {
        try {
            const payload = req.body;
            const result = await 
        } catch (error) {
            next(error)
        }
        
    }
}